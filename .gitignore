# 私鑰絕對不要進版控
*.key
linkedout_*.key

# 各機器自己的 agent list / 白名單（內含對方公鑰，屬個人設定）
agents.json

# 各機器自己的分享資料夾（read-only / read&append 的目標）
share/

DESIGN.md
__pycache__/
*.pyc
.conda/
.venv/
venv/

# 本地 AI 模型（用 ollama 管理，住在 ~/.ollama/，本目錄不該出現）
# 仍為了保險加上以下副檔名與資料夾，避免不小心 commit 上去
*.gguf
*.safetensors
*.bin
*.pt
*.pth
models/
.ollama/
