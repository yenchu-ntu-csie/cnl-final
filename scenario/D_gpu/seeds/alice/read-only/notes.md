# 量化筆記（Alice，ML / 模型量化）

【這台機器的實測甜蜜點 — 別人問不到、只有我們跑過才知道】
我們實驗室那台只有 12GB 顯存。實際跑了很多次才抓到這台的上限：
- 量化要用 **Q4_K_M**（其他變體在這張卡上不穩）。
- context 上限**只能設到 3500 tokens**；只要設到 4096 就一定 OOM 當掉。

所以這台的安全設定就是固定一組：**Q4_K_M + context 3500**。
（這是這台機器的實測值，通用教學不會告訴你「就是 3500 這個數」。）
